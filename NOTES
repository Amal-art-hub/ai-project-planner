❓ Question 1 (Full-Stack Architecture & Request Flow)
Can you trace the exact journey of a request in your application?

Specifically, from the moment a user clicks the "✨ Generate Project Plan" button in React, what happens step-by-step through your React frontend, your Express backend, the Gemini AI API, and back to the browser screen?


 =============ANS=======
"In my application, when the user clicks Generate Project Plan, the request follows a clean 5-step full-stack pipeline:

React Frontend: React captures the state variables (projectName, description, experience, technology, deadline) and sends an asynchronous HTTP POST request with a JSON body to our Express backend.
Express Controller: Express receives the request on Port 5000, validates the payload, and delegates prompt creation to a dedicated service layer (promptService.js).
Prompt Service Layer: promptService.js constructs a structured prompt combining the system role with the user's specific context.
External LLM Call: Express invokes the Google Gemini AI API (gemini-3.6-flash) over HTTPS using our secured environment key.
Response & UI Render: Gemini generates the architecture plan and returns it to Express, which forwards the JSON response back to React. React updates its state and dynamically renders the response on screen."*



//======================================================================================


❓ Question 2 (Security & Architecture)
Why did you choose to call the Gemini AI API from your Express backend instead of calling it directly from your React frontend?

What security risk or technical issue would happen if you put your GEMINI_API_KEY directly inside your React code?

===ANS===============

I chose to call the Gemini API from my Express backend because the Gemini API key is sensitive information and should not be exposed to the client.

If I put the `GEMINI_API_KEY` directly inside my React code, the code is ultimately delivered to the user's browser. A user could inspect the application's JavaScript, browser developer tools, or network requests and potentially extract the API key.

That could allow someone to misuse the key, make unauthorized API requests, and potentially cause unexpected API usage or costs.

So I keep the Gemini API key securely on the backend, usually in an environment variable. React sends the user's project data to my Express server, and the Express server uses the private API key to communicate with Gemini.

The backend therefore acts as a secure boundary between the frontend and the external AI service. It also gives me a central place to handle authentication, authorization, validation, rate limiting, error handling, and other business logic.

So the key principle is: **never expose sensitive API keys in frontend code; keep them on the server side.**

==============

💡 Why is CORS part of Topic 3?
Because when integrating an LLM into a web application, your React app (frontend) and Node server (backend) run on different origins/ports (5173 vs 5000).

Without understanding and configuring CORS, your browser will block the network flow, preventing your React app from ever communicating with your backend AI server!

=ans=====

The browser security policy involved here is the **Same-Origin Policy**, or SOP.

In my application, the React frontend runs on `http://localhost:5173` and the Express backend runs on `http://localhost:5000`. Even though they are both running on localhost, the ports are different, so they are considered **different origins**.

The browser therefore applies the Same-Origin Policy and does not allow the frontend to freely access responses from the backend unless the backend explicitly permits the cross-origin request.

To handle this, I used the **CORS middleware** in Express with `app.use(cors())`.

CORS stands for **Cross-Origin Resource Sharing**. It adds the appropriate CORS response headers to tell the browser that requests from another origin are allowed.

In my case, this allows my React application running on port 5173 to communicate with my Express backend running on port 5000.

So, simply:

**Same-Origin Policy = the browser's security rule**

**CORS = the mechanism that allows the server to grant permission for cross-origin requests.**


//======================



In Step 1B of your application, you created server/services/promptService.js and moved buildProjectPlanPrompt() out of server/index.js.

Why is it bad practice to write long prompt strings directly inside your main server route file (index.js)?
How does using a dedicated promptService.js help your application architecture as you add more AI features (like Task Lists, Risk Analysis, or Timelines)?


//=ans========

It is bad practice to put long prompt strings directly inside `index.js` because `index.js` should mainly be responsible for handling HTTP requests, routing, middleware, and sending responses.

If I put large AI prompts directly inside the route, the file can become difficult to read, maintain, maintain, and debug. It also mixes different responsibilities in the same file.

That is why I created a separate `promptService.js`. It is responsible for constructing and managing the prompts, while `index.js` focuses on handling the API request and response.

This becomes especially useful when I add more AI features. For example, instead of putting everything inside `index.js`, I can have separate prompt functions such as:

* `buildProjectPlanPrompt()`
* `buildTaskListPrompt()`
* `buildRiskAnalysisPrompt()`
* `buildTimelinePrompt()`

Then each route can call the appropriate prompt function.

This gives me **separation of concerns**, better code organization, easier testing, easier maintenance, and better scalability as the application grows.

So the main idea is:

**`index.js` → handles HTTP/API logic**

**`promptService.js` → handles AI prompt construction**

This keeps each part of the application focused on a single responsibility.




//==============


❓ Question 5 (Syllabus Topic 7.3 & 7.4: Guardrails & Fallback Strategies)
In your application code (src/App.jsx and server/index.js), how did you handle edge cases and system failures?

Frontend Validation: What happens if the user leaves the Project Name or Description blank and clicks Generate Project Plan?
Backend Fallback Strategy: What happens if the network drops or the Gemini AI API returns an error? How does your backend and frontend catch that error so the app doesn't crash?


==============ans================


🛡️ 1. Frontend Validation (Guardrails)
In your 
src/App.jsx
:

javascript


// Lines 17 - 20
if (!projectName.trim() || !description.trim()) {
  alert('Please fill in Project Name and Description.')
  return
}
What it does: If the user leaves the input fields blank, React stops the submission immediately. It doesn't send empty network requests to Express, saving server bandwidth and API quota.
Button Guardrail: Your button is also grayed out (disabled) until the user fills in the required fields!
🚨 2. Backend Fallback & Error Handling (try/catch)
In your 
server/index.js
:

javascript


// Lines 20 - 48
try {
  // Call Gemini AI
  const response = await ai.models.generateContent(...)
  res.json({ plan });
} catch (error) {
  console.error('AI Generation Error:', error);
  res.status(500).json({ error: 'Failed to generate plan from AI.' });
}
What it does: If Gemini AI is down, rate-limited, or fails, the try/catch block catches the error instead of letting Node.js crash. Express sends a clean 500 status with { error: "..." } back to React.
💻 3. Frontend Fallback Display (try/catch/finally)
In your 
src/App.jsx
:

javascript


try {
  // fetch request
} catch (error) {
  setResponse('Failed to connect to backend server. Make sure node index.js is running on port 5000.')
} finally {
  setLoading(false) // Always re-enables the button!
}
What it does: If the server fails or network disconnects, React catches the error and displays a friendly error message in the AI Response box. The finally block ensures the button stops saying "Generating..." and becomes clickable again!
🎙️ How to say it in an interview:
"We implement validation and error handling on both ends. On the frontend, we validate required inputs before triggering fetch(). On the backend, we wrap external LLM API calls in a try/catch block. If the AI service or network fails, the backend sends a 500 error payload, React catches it, displays a user-friendly error message, and the finally block resets the button loading state so the app never crashes or freezes."

9:35 PM



=================


"Can you explain your AI Project Planner project to me? Start from the moment a user enters the project details in the React frontend and explain the complete flow until the 
AI-generated project plan is displayed to the user."




My project is an AI-powered Project Planner that takes project requirements from the user and generates a structured development plan using Gemini.

First, the user enters five details in the React frontend: project name, project description, experience level, technology stack, and deadline.

When the user clicks "Generate Project Plan", the frontend first performs basic validation to make sure the required fields, especially project name and description, are filled.

Then React sends these details to my Express backend using an HTTP POST request to `/api/project/plan`. The data is sent as JSON.

The backend receives the request and performs another layer of input validation. This is important because frontend validation cannot be trusted by itself.

After that, the backend calls `generateMultiStepPlan()` from my `promptService.js`. I use a multi-step AI workflow instead of asking the AI to generate the entire plan in one 
call.

In the first step, I use a system prompt that instructs the AI to act as a senior software architect and analyze the project scope and complexity.

The result from step one is passed into the second AI call, where another system prompt instructs the AI to act as a technical project manager and generate development phases, 
tasks, and estimated timelines.

Then the third AI call uses the project information and generated phases to identify project risks and create a testing plan.

After these three steps, I merge the results into one final project-plan object.

Before sending this result to the frontend, my backend validates the generated plan using `validateProjectPlan()`. This makes sure that the AI has returned the required 
structure and expected data types.

If the validation succeeds, the backend sends the structured plan back to React as JSON.

Finally, React stores the plan in state and renders the project overview, complexity, estimated days, development phases, tasks, risks, and testing plan in the user interface.

So the overall flow is:

React frontend → Express backend → prompt construction → multi-step Gemini workflow → output validation → React frontend → user interface.


//===============================

In your project, what is the difference between the system prompt and the user/project data that you send to Gemini? Can you show me where each one is defined in your code and 
explain why you separate them?

===

The system prompt and the user/project data have different responsibilities.

The **system prompt is static**. It contains the instructions and rules that I want the AI to follow, such as how to analyze the project, what information to include, and that 
the final response should follow a specific JSON structure.

The **user/project data is dynamic**. It contains the actual requirements provided by the user, such as the project name, description, experience level, technologies, and 
deadline. This data changes for every request.

In my project, I keep the system instructions separate from the dynamic project data. I have an `analyzeSystemPrompt` for the static instructions, and then I construct the 
user/context data using the values submitted from the React frontend.

So conceptually, the request to Gemini is:

**System prompt:** "Here are the rules for how you should analyze and respond."

**User/project data:** "Here is the actual project I want you to analyze."

I separate them because the instructions should remain stable and reusable, while the project data changes for every user. This also makes the prompt construction easier to 
maintain and prevents mixing application rules with user-provided data.




//======================================================================


Question 3 — Context Injection

Interviewer:

You mentioned context injection. In your actual project, give me one concrete example of where information generated by the AI in one step is injected into another AI request. 
What is the purpose of doing that?

//==============




In my project, I use context injection between multiple chained AI calls.

First, the backend sends the project details to Gemini, and the first AI call analyzes the project scope and complexity.

Then I take the output from Step 1, specifically the project overview and complexity, and inject that information into the prompt for Step 2. Step 2 uses that context to 
generate the development phases, tasks, and estimated timeline.

Then I take the phases generated in Step 2 and inject them into Step 3. Step 3 uses that context to identify potential risks and create the testing plan.

So the output of one AI step becomes context for the next AI step. This helps each step focus on a specific responsibility while still maintaining the context of the overall 
project.


//==========================

Question 4 — Conversation Memory

Now let's test the conversation memory part of your syllabus.

Interviewer:

Your application also has an AI Assistant. Suppose the user first asks, "What should I implement first?" and then asks, "What about authentication?" How does your application
 remember the previous conversation and provide context to Gemini? Explain the flow using your conversationHistory state.

 =====
 In my AI Assistant, I maintain conversation memory using a React state called `conversationHistory`.

Whenever the user asks a question, I send the question along with the current project context, current project plan, and previous conversation history to the backend.

After receiving the AI's response, I store both the user's question and the assistant's answer in `conversationHistory`.

When the user asks the next question, the previous conversation history is sent again as part of the new request. On the backend, I format that history into text and include 
it in the Gemini prompt.

This allows the LLM to understand what was discussed previously and provide a context-aware response.

The important point is that the LLM doesn't automatically remember previous requests in this implementation. **My application maintains the conversation history and sends the 
relevant history with each new request.**


==========================

Question 5 — System Prompt vs Hidden Instructions

Interviewer:

You mentioned system prompts, user prompts, and hidden instructions. In your application, you explicitly define a systemInstruction when calling Gemini. What is the purpose of 
systemInstruction, and how is it different from putting all the instructions inside contents?

=====


I separated the system instruction from the contents because they have different responsibilities.

The `systemInstruction` contains relatively stable application-level instructions that define how Gemini should behave. For example, in my project I tell the model that it is 
a software project planning assistant and specify what it should analyze and what structure it should return.

The `contents` contain the dynamic, request-specific information, such as the project name, description, technology stack, experience level, and deadline.

Technically, the instructions and user data could be combined, but keeping them separate makes the prompt architecture clearer, more reusable, and easier to maintain. It also 
clearly distinguishes the rules the model should follow from the actual data it needs to process.


=========================
You mentioned hidden instructions as part of how LLM requests are internally wrapped. In your AI Project Planner, what do you understand by hidden instructions? Are they the 
same as your systemInstruction? And can you see or control every instruction that the Gemini model receives?

//========

In my AI Project Planner, I understand hidden instructions as instructions that influence the AI's behavior but are not exposed to the end user.

In my project, I explicitly use Gemini's `systemInstruction`. It contains my application's rules, such as how the AI should behave and how it should generate the response. The 
end user doesn't see this instruction, but as the developer, I can see and control it.

However, `systemInstruction` and hidden instructions are not exactly the same concept. `systemInstruction` is the specific mechanism I use to provide system-level instructions 
to Gemini, while hidden instructions is a broader concept referring to instructions that aren't visible to the end user.

I can see and control the instructions and context that my application sends to Gemini, such as my `systemInstruction`, project context, current plan, conversation history, 
and user question. But I cannot see or control every internal instruction, safety mechanism, or processing that Gemini itself may use.

So, I control my application's instructions, but I don't control everything that happens inside the Gemini model or platform.
//==================

1. config isn't just "updating an object property."

It's a configuration object for the Gemini request. In your project, you use it for things such as:

config: {
  systemInstruction: assistantSystemPrompt,
  responseMimeType: 'application/json'
}

So you can say:

"config contains additional generation/request configuration, such as the system instruction and response format."

=========================================



The `model` specifies which Gemini model I want to use for the request.

The `contents` contain the request-specific information. In my AI Assistant, this includes the project context, current project plan, conversation history, and the user's 
current question.

The `systemInstruction` contains the stable instructions that define how the AI should behave, such as acting as a project planning assistant and giving practical, concise 
answers.

The `config` object contains additional request configuration. In my project, I use it for things like `systemInstruction` and, in the planning workflow, `responseMimeType: 
'application/json'` to request structured JSON output.

Conceptually, the SDK sends these structured request components to Gemini. Gemini uses the system instructions together with the provided contents and generates a response 
using the specified model. My backend then receives the response and processes it.


//==============================================================================================================================================


Interview Question 1 — User Prompt vs System Prompt

Interviewer:

In your AI Project Planner, you have ANALYSIS_SYSTEM_PROMPT and you also send project information through contents. Why did you put the project-planning rules in the system 
prompt instead of putting them together with the project information? What would be the difference if you put everything into contents?



//=============

I separate them because they have different responsibilities.

The system prompt contains relatively stable application-level instructions and rules that define how the AI should behave. For example, my `ANALYSIS_SYSTEM_PROMPT` tells 
Gemini to act as a senior software architect and return the project overview and complexity in a specific structure.

The contents contain dynamic, request-specific project information such as the project name, description, technology stack, and deadline.

Keeping them separate makes the prompt easier to maintain, reuse, and control. It also clearly separates the AI's instructions from the data that the AI needs to process.

In my project, I use dynamic prompt construction with template literals to insert the user's project data. I use context injection specifically when I pass the output from one 
AI step into the next step of my multi-step pipeline.


//================================


Question 2 — Designing Prompt Templates

Interviewer:

Your promptService.js contains several prompts such as ANALYSIS_SYSTEM_PROMPT, PHASES_SYSTEM_PROMPT, and RISKS_TESTING_SYSTEM_PROMPT. If you had to design a reusable prompt 
template for your application, how would you structure it so that the instructions stay fixed while the project information changes for every request?


//=====
I designed my prompts as separate, reusable system prompts for each stage of the AI workflow.

In my current implementation, I have three prompt templates: `ANALYSIS_SYSTEM_PROMPT`, `PHASES_SYSTEM_PROMPT`, and `RISKS_TESTING_SYSTEM_PROMPT`.

Each template contains relatively stable instructions, a specific role, a specific task, and the expected output structure. The dynamic project information is passed 
separately through `contents`, such as the project description, experience level, technology stack, and deadline.

For example, the first template is responsible for analyzing the project scope and complexity. The second uses that analysis as context and generates development phases and 
tasks. The third uses the generated plan to identify risks and create the testing checklist.

I also have a `buildUserPrompt()` function in my code from an earlier version, but my current multi-step workflow does not use that function. Instead, the current 
implementation constructs the input for each AI step directly inside `generateMultiStepPlan()`.

This approach keeps each prompt focused, easier to maintain, and reusable for its specific stage of the AI pipeline.


==================================================================


Question 3 — Consistent Outputs

Interviewer:

In your current three-step AI pipeline, how do you make Gemini's responses consistent and predictable? Look at your code and identify the specific things you implemented to 
control the output format and structure.

//===============


In my current three-step AI pipeline, I make the AI output more consistent and predictable using several techniques.

First, each AI step has a dedicated system prompt that defines its role, task, and expected output structure. I have separate prompts for project analysis, development phases, 
and risks and testing.

Second, I use `responseMimeType: 'application/json'` in the Gemini configuration to request structured JSON output instead of unrestricted text.

Third, after the AI responses are parsed using `JSON.parse()`, I validate the final project plan using `validateProjectPlan()` before sending it to the frontend.

So, the system prompts provide clear instructions, the JSON response configuration controls the expected format, and output validation acts as a guardrail against malformed 
responses.


//==================================================
Interview Question 4 — Multi-Step Prompt Pipeline

Interviewer:

Your application uses a three-step AI pipeline instead of making one large Gemini call. Explain why you designed it this way.

Specifically, walk me through what Step 1, Step 2, and Step 3 do, and explain how the output of one step is used by the next step.
============

In my application, I use a three-step AI pipeline instead of making one large AI call. I designed it this way to separate responsibilities and make the outputs more structured, consistent, and predictable.

In Step 1, I use a system prompt that gives the AI the role of a senior software architect. Its task is to analyze the project details and return the project overview and 
complexity as JSON.

I then take the output from Step 1 and inject it as context into Step 2. In Step 2, I give the AI the role of a technical project manager. It uses the project overview, 
complexity, experience level, technology stack, and deadline to generate realistic development phases, tasks, and estimated timelines.

Then I take the relevant output from Step 2 and use it as context for Step 3. In Step 3, I give the AI the role of a QA and risk management expert. It analyzes the development 
plan and generates the project risks and testing checklist.

Finally, I merge the outputs from all three steps into one `finalPlan` object and return that plan to the backend route, which sends it to the React frontend.

The main advantage is that each step has a focused responsibility, which makes the workflow easier to control, maintain, and validate.


//=======================================================


Question 2

Interviewer:

You mentioned that you're sending JSON from the React frontend to the Express backend. Can you explain exactly what happens when you use JSON.stringify() in your fetch() 
request, and why you need it?


//=========

In my application, I integrated Google's Gemini LLM using a React frontend and an Express.js backend.

First, the user enters five project details in the React form: project name, description, experience level, technology stack, and deadline. When the user clicks the "Generate 
Project Plan" button, the `handleGeneratePlan` function is triggered.

Inside this function, I first perform frontend validation for the required fields, mainly the project name and description. If the required fields are available, I create the 
request body containing all five project details and convert it into JSON using `JSON.stringify()`. Then I send it to my Express backend using `fetch()` with a POST request and `Content-Type: application/json`.

On the backend, Express receives the request through the `/api/project/plan` endpoint. I validate the required fields again on the server because frontend validation cannot be 
trusted.

Then I pass the project details to my `generateMultiStepPlan()` function in the prompt service. I have initialized the Gemini client using the API key stored in the `.env` file,
 so the API key is not exposed in my frontend code.

Instead of making one large LLM call, I use a three-step chained AI workflow. In the first step, Gemini analyzes the project scope and complexity. In the second step, it uses 
the first step's output to generate development phases, tasks, and estimated timelines. In the third step, it uses the previous information to identify project risks and create a testing checklist.

Each step has its own system prompt that defines the AI's role, task, and expected JSON structure. I also request JSON responses from Gemini.

After all three steps are completed, I merge their outputs into one final project plan object. Before sending it to the frontend, I validate the structure of that final 
AI-generated plan.

If the plan is valid, the Express server sends it back to React as JSON using `res.json({ plan })`.

Finally, React receives the response using `res.json()`, stores the plan in the `plan` state using `setPlan(data.plan)`, and React re-renders the UI to display the project 
overview, complexity, estimated days, development phases, risks, and testing plan.

So the overall flow is:

React frontend → Express backend → Gemini multi-step pipeline → output validation → Express response → React state → UI.


//=================================================================================


🎯 Interview Question 2

“How are you handling the API-based AI integration in your application?”

Explain specifically how you:

Initialize the Gemini client
Keep the API key in .env
Send the project data from React to your Express backend
Call Gemini from the backend
Receive the AI response
Send the result back to React

Answer in your own words, and I’ll correct it into an interview-ready answer.

//=======
“I integrated Gemini through my Express backend. The React frontend sends the user's project details to the backend using a POST request. The backend uses the Gemini client, 
initialized with an API key stored in .env, to call the LLM. I process and validate the AI-generated JSON response on the backend, then return the structured plan to React as 
JSON. React stores that response in state and uses it to render the project plan in the UI.”



//==================================================================================


🎯 Next Interview Question — Question 4

“How do you structure the AI response so that it can be easily displayed in your React UI?”

//==============


I manage the input and output flow between the frontend, backend, and Gemini in multiple steps.

First, when the user enters the project details, React stores them in state. When the user clicks Generate Project Plan, I send those details to the backend using `fetch()` 
with a POST request. I use `JSON.stringify()` to convert the JavaScript object into a JSON string for the request body.

When Express receives the request, I have configured `app.use(express.json())`. This middleware parses the JSON request body, so `req.body` becomes a JavaScript object. I can 
then access the individual project details from `req.body`, such as `projectName`, `description`, `experience`, `technology`, and `deadline`.

These project details are then passed to my `generateMultiStepPlan()` function in the prompt service.

Inside the prompt service, I use a three-step chained AI workflow. In each step, I instruct Gemini to return the response in JSON format. I then use `JSON.parse()` to convert 
the JSON response into a JavaScript object.

The output from one step is used as relevant context for the next step. For example, the project overview and complexity generated in Step 1 are passed into Step 2 to generate 
development phases and tasks.

After all three steps are completed, I combine their outputs into one final `finalPlan` object. Before sending it to the frontend, the backend validates the structure using 
`validateProjectPlan()`.

Finally, the backend sends the validated plan as JSON to React using `res.json()`. React receives the response using `res.json()` on the frontend and stores the plan using 
`setPlan()`. The plan is then used to render the structured project information in the UI.


//=======================================================================================


🎯 Interview Question 4

“How do you structure the AI response so that it can be easily displayed in your React UI?”

Answer this based on your actual code, especially:

responseMimeType: 'application/json'
JSON.parse()
finalPlan
validateProjectPlan()
setPlan(data.plan)
plan.projectOverview
plan.phases.map()
plan.risks.map()
plan.testingPlan.map()

Try answering it yourself first, like you're speaking to the interviewer.

//=======================

When I receive the AI-generated response from the backend, I structure it as a JSON object so that the frontend can easily work with it.

In my backend, I configure Gemini with `responseMimeType: 'application/json'`, so each AI step is expected to return JSON. I use `JSON.parse()` in the backend to convert the 
AI response into JavaScript objects.

Then I combine the outputs from the three steps into one `finalPlan` object containing fields such as `projectOverview`, `complexity`, `estimatedTotalDays`, `phases`, `risks`,
 and `testingPlan`.

I validate this final plan using `validateProjectPlan()` before sending it to the frontend.

The backend then sends it using `res.json({ plan })`. On the React side, I use `const data = await res.json()` to parse the HTTP response, and then I store the plan using 
`setPlan(data.plan)`.

Because the response has a predictable structure, I can directly access properties like `plan.projectOverview` and `plan.complexity`, and use `.map()` on arrays such as 
`plan.phases`, `plan.risks`, and `plan.testingPlan` to render them in the UI.

So, the main idea is to return a consistent structured JSON response from the AI and backend, which makes it easy for React to display the data.




///=========================================================================
🎯 Question 5

“Why did you choose to return the AI response as structured JSON instead of returning plain text?”

//=============
I chose structured JSON because my frontend needs to work with different parts of the AI response separately.

In my application, the project plan contains different fields such as `projectOverview`, `complexity`, `estimatedTotalDays`, `phases`, `risks`, and `testingPlan`.

If I returned plain text, I would have to manually parse that text to extract each section. With structured JSON, I can directly access each property and render it in the UI.

For example, I can use `plan.projectOverview` to display the overview and `plan.phases.map()` to display the development phases and tasks.

It also gives me a predictable response structure, which makes validation easier and makes the communication between my backend and frontend more reliable.

So, I use JSON because it provides a structured, predictable format that my React application can easily process and render.


//====================================================


What happens if Gemini returns an invalid or malformed response in your application? How do you handle it?”

//===============

If Gemini returns an invalid or malformed response, I handle it on the backend before sending it to the frontend.

First, because I expect Gemini to return JSON, I use `JSON.parse()` to convert the response into a JavaScript object. If the response is not valid JSON, `JSON.parse()` will 
throw an error, which is caught by my `try-catch` block and the server returns an error response.

After parsing, I also validate the structure of the final plan using my `validateProjectPlan()` function. It checks whether the plan is an object and whether required fields 
like `projectOverview`, `complexity`, and `phases` exist with the expected types.

If the validation fails, I return a `502` response with an error message instead of sending malformed data to the frontend.

This prevents my React UI from trying to render an unexpected AI response and gives me a controlled way to handle malformed AI output.

//=============================================================


“Why are you calling Gemini from the backend instead of directly from your React frontend?”

//================



I call Gemini from the backend instead of directly from the React frontend mainly for security and better control.

The most important reason is that my Gemini API key is a secret, so I should not expose it in the frontend code. In my application, I store the API key in the backend `.env` 
file and initialize the Gemini client in `server/index.js`.

The backend also gives me a central place to control the AI integration. I can build prompts, manage the three-step AI workflow, validate the AI response, handle errors, and
apply guardrails before sending the result to the frontend.

So the architecture is:

React frontend → Express backend → Gemini → Express backend → React frontend.

This keeps the API key protected and allows the backend to control and validate the AI input and output.\



//===========================================================

What is the difference between a normal prompting approach and an agentic workflow?

//=================

**“In normal prompting, we give the AI a specific prompt and it generates a response based on that prompt. In an agentic workflow, the AI is given a goal and can determine the 
steps needed to achieve that goal, including using tools or external data and taking actions.**

**In my application, I use a predefined three-step AI workflow rather than a fully agentic workflow, because my backend controls the sequence of Gemini calls. Each step has a 
specific task, and the output of one step is passed to the next step.”**




//=============================================================

What do you mean by a tool-connected AI system? Can you give an example related to your application?

//=====================



**“A tool-connected AI system is an AI system that can use external tools or services to complete a task. For example, if the user asks the AI to perform a calculation, the AI 
can call a calculator tool, receive the result, and then use that result to generate the final response.**

**In my application, I haven't connected any external tools yet. My application mainly uses Gemini to generate a project plan. However, I could extend it by connecting tools 
such as Jira. For example, after generating the project tasks, the AI could use a Jira tool or API to create those tasks in a Jira project.”**




//========================================================


What is MCP (Model Context Protocol), and why would we use it in an AI application?

//===================


**“MCP stands for Model Context Protocol. It is an open protocol that provides a standardized way for AI applications to connect with external tools and data sources. It helps 
AI systems interact with different services without requiring a completely different integration approach for every tool.**

**For example, in my Project Planner application, I could use MCP to connect the AI with a service like Jira, so the AI could use Jira capabilities such as creating or 
retrieving project tasks.”**


//=======================================================


How can AI use external tools or data to complete a task?

//================


**“AI can use external tools or data sources to complete tasks that require capabilities outside the AI model itself. The AI can request an external tool to perform an 
operation, receive the result, and then use that result to complete the user's request.**

**For example, in my Project Planner application, Gemini generates the project plan and breaks the project into tasks. I could then connect a calendar tool so that the AI can 
schedule those tasks based on the project deadline. The calendar tool would handle the actual scheduling, while Gemini would use the result to provide the user with the final 
plan.”**


//==================================================


"What is hallucination and how do you handle it?"


//=============



"AI hallucination is when an AI model generates information that sounds correct but is actually false or unsupported. I can reduce hallucinations by using clear prompts, 
providing reliable context, using RAG or external tools when factual information is required, and validating the model's structured output. In my project, I use structured 
JSON responses and validateProjectPlan() to make sure the generated project plan follows the expected structure."
Good response: "I chose not to implement hallucination detection because:

My application is a planning tool, not a fact-based system
Project planning is subjective - there's no single 'correct' answer
Users are expected to review and modify AI suggestions
The AI provides starting points, not definitive answers
I focused on structural validation (JSON schema, required fields) which is more appropriate for this use case
For critical applications (medical, financial, legal), hallucination detection would be essential, but for creative planning, human judgment is the primary safeguard"

//======================================================


Interview — Question 1

Imagine you have demonstrated your application to me. I look at your promptService.js and see that you're getting project plans from Gemini.

Interviewer:

"Your application depends on an LLM to generate the project plan. What happens if Gemini gives you an incorrect or made-up response — for example, it recommends a technology 
that doesn't actually exist or gives an unrealistic development plan? This is called hallucination. How would you handle hallucinations in your application, especially in a 
production environment?"

//===============

"LLM hallucination means the model can generate information that sounds correct but is actually false or unrealistic. In my application, I handle this using multiple approaches.

First, I validate the AI output before using it. For example, in my project I have a validateProjectPlan() function that checks whether the generated response has the expected structure, required fields, and correct data types.

Second, I use structured JSON output and clear system instructions to constrain the model's response and make it more predictable.

Third, for production, I could add additional validation rules or external tools when factual verification is important. For example, if the AI recommends a technology, an 
external knowledge source could verify whether that technology actually exists or is supported.

Finally, I would use a fallback strategy. If the AI response fails validation or cannot be trusted, I would not directly show it to the user. I could return an error, retry 
the request with stricter instructions, or provide a safe default response.

Because my application generates project plans, there can be multiple valid approaches, so I don't try to validate whether there is one 'correct' plan. Instead, I validate the 
structure, constraints, feasibility, and important factual information before using the output."



//=======
"I'm already doing output validation. After my three-step Gemini workflow produces the final plan, I pass it through validateProjectPlan(). It checks that the response is an 
object, that projectOverview and complexity are strings, and that phases is a non-empty array. If the validation fails, my backend doesn't send that malformed plan to the 
frontend; it returns an error instead.